# AutoSCM AI

AutoSCM AI is a lightweight supply chain decision-support app built with Streamlit.

## Features
- Loads demand, inventory, and supplier data
- Computes days of cover and reorder risks
- Generates simple forecasts
- Recommends actions
- Uses Ollama for AI summaries

## Run
```bash
streamlit run app.py